#!/usr/bin/env node
'use strict';

module.exports = {
  revise: revise,
  makeParentName: makeParentName,
  makeProjectName: makeProjectName
};

function revise(moduleUnrevised) {
  
}

function makeParentName(moduleRevised, projectPath) {

}

function makeProjectName(moduleRevised, projectPath) {

}
